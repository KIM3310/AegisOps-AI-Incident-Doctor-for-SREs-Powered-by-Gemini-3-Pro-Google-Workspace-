apiVersion: v1
kind: Service
metadata:
  name: aegisops-api
  namespace: aegisops
  labels:
    app: aegisops
    component: api
spec:
  type: ClusterIP
  selector:
    app: aegisops
    component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
